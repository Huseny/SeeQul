import 'package:flutter/material.dart';

class Zaady extends StatelessWidget {
  const Zaady({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Zaady")),
    );
  }
}
